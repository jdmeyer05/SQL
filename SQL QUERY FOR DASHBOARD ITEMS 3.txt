//SQL QUERY FOR DASHBOARD ITEMS 3//
SELECT


SUM(CASE WHEN TRANSACTION_COMPONENT = 'GROSS' AND COMPONENT_AMT > 0 THEN COMPONENT_AMT END),
SUM(CASE WHEN TRANSACTION_COMPONENT = 'TRANS' AND COMPONENT_AMT > 0 THEN COMPONENT_AMT END),
SUM(CASE WHEN TRANSACTION_COMPONENT = 'SEV' AND COMPONENT_AMT > 0 THEN COMPONENT_AMT END),
SUM(CASE WHEN TRANSACTION_COMPONENT = 'NET' AND COMPONENT_AMT > 0 THEN COMPONENT_AMT END)

FROM

EULAPPS.P2_REV_ACCRUAL_TRANSACTIONS_V


WHERE


RC_USER_KEY = 























































































































































































































































































































































